// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface SkillCardProps {
  title: string;
  thumbnail: string;
}

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface SkillCardPresentationProps {
  title: string;
  thumbnail: string;
}
