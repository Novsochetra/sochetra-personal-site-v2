import React from 'react'
import { SectionHeader as SectionHeaderComponent } from '.'
import "./SectionHeader.module.css"

export default { title: 'SectionHeader' }

export const SectionHeader = (): React.ReactElement => {
  return <SectionHeaderComponent />
}
